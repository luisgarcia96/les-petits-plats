/* 
Create a function that verifies if the item selected 
is already in the tag list. return true if so, false if not

This function will be added as a middleware in the index.js file 
when cliking on an item 
*/
export function isTagAlreadySelected(tag) {
    const selectedTagsSection = document.querySelector('.selected-tags');
    console.log(selectedTagsSection);
}